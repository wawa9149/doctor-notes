# 개발용 Dockerfile
FROM node:18-alpine

WORKDIR /app

# package.json과 package-lock.json 복사
COPY package*.json ./

# 모든 의존성 설치 (개발 의존성 포함)
RUN npm ci

# 소스 코드 복사
COPY . .

# 환경변수 파일 복사 (개발용)
COPY env.dev .env

# 개발 서버 포트 노출
EXPOSE 5173

# 개발 서버 시작
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"] 